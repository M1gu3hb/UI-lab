{
  "name": "ui-lab",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Morphiq Material Lab — cocina de recetas de material para Morphiq UI",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "preview": "python3 -m http.server 4173 -d dist"
  }
}
